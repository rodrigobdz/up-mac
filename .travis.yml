os: osx
osx_image: xcode10.2
language: bash
cache:
  npm: true
  yarn: true
  directories:
    - $HOME/Library/Caches/Homebrew
before_cache:
  - brew cleanup
addons:
  homebrew:
    packages:
      - shellcheck
script:
  - shellcheck up-mac
  - HOMEBREW_NO_AUTO_UPDATE=1 bash up-mac
